import Layout from '../components/layout';

const ContactUs: React.FC = () => {
  return (
    <Layout>
      <h1 style={{ minHeight: 'calc(100vh - 12.5rem)' }}>Contact Us Page</h1>
    </Layout>
  );
};

export default ContactUs;
