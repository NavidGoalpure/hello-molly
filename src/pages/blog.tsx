import Layout from '../components/layout';

const Blog: React.FC = () => {
  return (
    <Layout>
      <h1 style={{ minHeight: 'calc(100vh - 12.5rem)' }}>Blog Page</h1>
    </Layout>
  );
};

export default Blog;
