import Layout from '../components/layout';

const Services: React.FC = () => {
  return (
    <Layout>
      <h1 style={{ minHeight: 'calc(100vh - 12.5rem)' }}>Services Page</h1>
    </Layout>
  );
};

export default Services;
